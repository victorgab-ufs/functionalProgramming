// área do retângulo

// definicao da funcao de calculo da area de retangulo currificada
             // | parametros |     | corpo da funcao |
const area = (base) => (altura) => base*altura

// chamada da funcao
const resultado = area(2)(2)

console.log(resultado)
