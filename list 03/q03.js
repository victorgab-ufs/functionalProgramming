// número de elementos

// funcao que recebe lista como argumento e retorna a quantidade de elementos presentes nela
const tamanho = (lista) => `A lista contém ${lista.length} elementos`
// chamada da funcao
const resultado = tamanho([10, 30, 50])
// saida
console.log(resultado)
